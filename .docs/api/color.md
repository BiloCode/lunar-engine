# Color

An RGBA color.

## Constructor

```ruby
Color.new(r, g, b)
Color.new(r, g, b, a)
```

- `r` — `Integer` (0–255) — red component
- `g` — `Integer` (0–255) — green component
- `b` — `Integer` (0–255) — blue component
- `a` — `Integer` (0–255, optional) — alpha component
