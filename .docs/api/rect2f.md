# Rect2f

A rectangle with floating-point components.

## Constructor

```ruby
Rect2f.new
Rect2f.new(x: 0.0, y: 0.0, w: 0.0, h: 0.0)
```

- `x` — `Float` — X position
- `y` — `Float` — Y position
- `w` — `Float` — width
- `h` — `Float` — height
