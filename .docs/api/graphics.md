# Graphics (Module)

Module that acts as a texture factory. Manages an internal `ImageManager`.

## Methods

| Method | Returns |
|---|---|
| [`load`](#load) | `Image` |

---

### load

```ruby
Graphics.load(key)
```

Loads a texture by key.

- `key` — `String` — texture identifier
