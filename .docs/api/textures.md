# Textures (Module)

Module that acts as a texture factory. Manages an internal `TextureManager`.

## Methods

| Method | Returns |
|---|---|
| [`get`](#get) | `Texture` |

---

### get

```ruby
Textures.get(key)
```

Loads a texture by key.

- `key` — `String` — texture identifier
