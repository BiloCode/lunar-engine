# Texture

Represents a loaded texture. Cannot be instantiated directly — obtain instances via `Textures.get`.

## Constructor

```ruby
Texture.new
```

Raises `RuntimeError`. Textures are created through the `Textures` module.
